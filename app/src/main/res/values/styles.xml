<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="theme_switch" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#ffe693</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#f1f1f1
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#ffe693
        </item>
    </style>
    <style name="NavigationDrawerStyle">
        <item name="android:textSize">11sp</item>
        <item name="android:textColor">#d3d3d3</item>
        <!--<item name="android:letterSpacing">0.15</item>-->
        <item name="android:lineSpacingExtra">4sp</item>
    </style>
    <style name="MenuItems" parent="AppTheme">
        <item name="android:textSize">11sp</item>
        <!--<item name="android:letterSpacing">.15</item>-->
        <item name="android:lineSpacingExtra">4sp</item>
    </style>
</resources>
